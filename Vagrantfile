# Vagrantfile for testing the box

Vagrant.configure(2) do |config|
  config.vm.box = "debian91"
  config.vm.box_url = "https://github.com/korekontrol/packer-debian8/releases/download/ci-8/debian91.box"
end
