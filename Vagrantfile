# Vagrantfile for testing the box

Vagrant.configure(2) do |config|
  config.vm.box = "debian94"
  config.vm.box_url = "https://github.com/korekontrol/packer-debian9/releases/download/ci-13/debian94.box"
end
